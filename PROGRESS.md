# WikiMind — Implementation Progress

> Following: `plan-v2.md` (lean implementation)
> Started: 2026-04-09

---

## Phase 1A — Working Ingest (Week 1)

Goal: `wikimind init && wikimind ingest .wiki/raw/article.md` works end-to-end.

| Task | Status | Notes |
|------|--------|-------|
| Project scaffold + venv | ✅ Done | `pyproject.toml`, `.venv/`, directory structure |
| `wikimind/wiki.py` — WikiStore | ✅ Done | Read/write pages, index, log, dedup |
| `wikimind/llm.py` — LLMClient | ✅ Done | Anthropic tool_use, token tracking |
| `wikimind/config.py` | ✅ Done | Load `wikimind.toml`, typed dataclasses |
| `wikimind/prompts/ingest.py` | ✅ Done | System prompt + INGEST_TOOL schema |
| `wikimind/operations/ingest.py` | ✅ Done | Full ingest flow |
| `wikimind/cli.py` — init + ingest | ✅ Done | Typer CLI |
| `wikimind/templates/general/` | ✅ Done | wikimind.toml, CLAUDE.md, index.md, overview.md |
| Tests: wiki + ingest | ✅ Done | Mocked LLM |

**Milestone:** `wikimind init → ingest raw/article.md → wiki pages created`

---

## Phase 1B — Working Query + Lint (Week 2)

| Task | Status | Notes |
|------|--------|-------|
| `wikimind/prompts/query.py` | ✅ Done | System prompt + QUERY_TOOL schema |
| `wikimind/operations/query.py` | ✅ Done | Full query flow + --save write-back |
| `wikimind/operations/lint.py` | ✅ Done | Structural checks + --fix |
| `wikimind/cli.py` — query + lint + status + cost | ✅ Done | Complete CLI |
| Tests: query + lint | ✅ Done | Mocked LLM |
| Large file handling (>50K chars) | ✅ Done | Chunk + summarize in ingest flow |
| Error handling: API failures, missing config | ✅ Done | Handled in LLMClient + CLI |

**Milestone:** Query answers wiki, lint catches issues, status shows stats

---

## Phase 2 — MCP Server (Week 3)

| Task | Status | Notes |
|------|--------|-------|
| `wikimind/server.py` — FastMCP | ✅ Done | `wiki_search`, read/write/index/log/status tools |
| `wikimind serve` CLI command | ✅ Done | `wikimind serve --transport stdio|sse` |
| `.mcp.json` generated by `wikimind init` | ✅ Done | Claude Code MCP config auto-generated |
| MCP path safety hardening | ✅ Done | Blocks `../` traversal on read/write paths |
| Tests: MCP server tools | ✅ Done | Tool registration + read/write + traversal guard |

---

## Phase 3 — Refinement

| Task | Status | Notes |
|------|--------|-------|
| Typed tool-output validation | ✅ Done | Ingest/query/lint parse + validate before file writes |
| Batch ingest failure semantics | ✅ Done | Per-run summary + non-zero exit when any file fails |
| Path-aware wikilink resolution | ✅ Done | Link registry supports explicit paths, avoids stem collisions |
| LLM provider adapter interface | ✅ Done | Adapter boundary + Anthropic/OpenAI/Ollama adapters |
| Retrieval abstraction boundary | ✅ Done | Retriever interface + `index_keyword` + `bm25` backends |
| BM25 search | ✅ Done | Real Okapi BM25 full-text retriever over wiki pages |
| MCP `wiki_search` uses configured retriever | ✅ Done | No longer hardcoded to index_keyword; respects `retrieval_backend` config |
| Multiple templates (code, research, book) | ✅ Done | `wikimind init --template code\|research\|book` |
| Lint semantic checks | ✅ Done | `wikimind lint --semantic` (report-only findings) |
| Stale sources lint check | ✅ Done | `wikimind lint` detects raw files changed since last ingest |
| Multi-provider support | ✅ Done | OpenAI + Ollama providers added |
| Budget guard | ✅ Done | `max_budget_usd` in `[llm]` config; enforced per-session in `LLMClient` |
| Persistent cost tracking | ✅ Done | `.wikimind/cost.json` logs every command; `wikimind cost` shows history + totals |
| PDF support | ✅ Done | `_read_source()` routes `.pdf` via `pymupdf4llm`; friendly error if dep missing |
| File watcher | ✅ Done | `wikimind watch [--interval N]` — polls raw/ and auto-ingests new/stale files |
| PyPI publish | ⬜ Pending | |

---

## Session Log

### 2026-04-09
- Created project scaffold, venv, pyproject.toml
- Implemented WikiStore, LLMClient, config loader
- Implemented ingest + query + lint operations
- Implemented CLI (init, ingest, query, lint, status, cost)
- Created templates/general/ (wikimind.toml, CLAUDE.md, index.md, overview.md)
- Written tests with mocked LLM
- Implemented MCP server + CLI `serve` + `.mcp.json` generation
- Hardened wiki path handling to prevent path traversal
- Added MCP server test coverage
- Normalized source dedup keys to handle relative/absolute path styles consistently
- Added regression tests for dedup path-key consistency
- Implemented semantic lint pass (single LLM call) and CLI `wikimind lint --semantic`
- Added semantic lint test coverage
- Added typed validation for ingest/query/lint tool outputs before applying changes
- Added path-aware wikilink registry and collision-safe resolution
- Added retrieval backend abstraction (`index_keyword`) to decouple operations from store search
- Refactored LLM client to provider adapter architecture (Anthropic adapter)
- Updated batch ingest CLI to report success/skip/fail counts and fail fast at exit code level
- Implemented real BM25 retriever backend and wired `retrieval_backend = "bm25"`
- Added OpenAI provider adapter (tool-calling via Chat Completions API)
- Added Ollama provider adapter (schema-constrained JSON via `/api/chat`)
- Switched default init layout to hidden `.wiki/` root (`.wiki/raw/`, `.wiki/vault/`, `.wiki/.wikimind/`)

### 2026-04-09 (continued)
- Fixed MCP `wiki_search` to use configured retrieval backend (BM25) instead of hardcoded index_keyword
- Added templates for `code`, `research`, and `book` use cases (`wikimind init --template <name>`)
- Implemented stale sources lint check: detects raw files changed since last ingest
- Wired PDF support: `.pdf` files routed through `pymupdf4llm` in ingest; friendly error if dep missing
- Added persistent cost tracking: `.wikimind/cost.json` accumulates across sessions; `wikimind cost` shows full history
- Added budget guard: `max_budget_usd` in `[llm]` config blocks LLM calls once session spend exceeds limit
- Added `wikimind watch` command: stdlib polling loop, auto-ingests new and stale files from raw/
